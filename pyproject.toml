[tool.ruff]
target-version = "py311"
line-length = 120
exclude = ["notebooks", "notebook_previews"]

[tool.ruff.lint]
# 教程质量门禁聚焦会影响执行正确性的诊断：语法错误、非法结构和未定义名称。
# 版式由 Ruff formatter/编辑器负责，不让教学注释因纯风格规则产生噪音。
select = ["E9", "F63", "F7", "F82"]
