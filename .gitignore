.venv/
__pycache__/
.pytest_cache/
.ipynb_checkpoints/
*.pyc
.ruff_cache/
# Paper PDFs are tracked under resources/papers/ so the in-site reader works
# right after clone. Full datasets, the EmbedPDF viewer bundle and the generated
# resource lock stay ignored; the download module fetches only missing items.
# The tracked manifest lives at config/resources.json.
resources/datasets/
resources/vendor/
resources/resource-lock.json
tmp/
.idea/
*.iml
.slim/deepwork/
